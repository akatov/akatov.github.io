---
title: Dmitri 5
layout: MainLayout.11ty.tsx
---

import { Counter } from './counter'
import { Heading } from '../src/components/Heading'

export function MyComp(props) {
  return <span>My Component</span>
}

## Dmitri 5 {props.title}

mdx inside tsx template is problematic abracadabra

This is <MyComp />.

And this is <Counter />.

And this is:

<Heading />

export const serializeEleventyProps = (props) => ({
  startCount: props.startCount,
});
